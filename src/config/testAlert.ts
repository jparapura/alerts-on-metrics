import { AlertConfig } from '../models/alert';

const testAlertConfig: AlertConfig = {
  name: 'power_availability for lenartowicza_243',
  okThreshold: 10,
  errorThreshold: 0,
};

export default testAlertConfig;
